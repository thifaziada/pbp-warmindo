import { ReactNode } from 'react';
interface ExampleSectionProps {
    name: string;
    withoutSpacingOnContent?: boolean;
    children: ReactNode;
}
declare const ExampleSection: ({ name, withoutSpacingOnContent, children, }: ExampleSectionProps) => JSX.Element;
export default ExampleSection;
