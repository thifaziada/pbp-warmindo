import { ThemeType } from '../../theme';
import { CheckboxProps, CheckboxStates } from './checkbox.type';
/**
 * computed style
 *
 * @param theme
 * @param props
 */
export declare const getStyle: (theme: ThemeType, props: CheckboxProps, extraProps: CheckboxStates) => any;
